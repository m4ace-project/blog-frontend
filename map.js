import React from 'react'


    const numbers = [1, 2, 3, 4, 5];

// Use map to square each number
const squaredNumbers = numbers.map(num => num * num);

console.log(squaredNumbers); //apply to it
 


